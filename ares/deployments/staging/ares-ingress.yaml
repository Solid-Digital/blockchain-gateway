apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  namespace: ares-staging
  name: ares-ingress
  annotations:
    certmanager.k8s.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/rewrite-target: "/"
    nginx.ingress.kubernetes.io/proxy-body-size: 200m
spec:
  tls:
    - secretName: "ares.staging.unchain.io"
      hosts:
        - "ares.staging.unchain.io"
  rules:
    - host: "ares.staging.unchain.io"
      http:
        paths:
          - path: "/"
            backend:
              serviceName: ares-service
              servicePort: default
